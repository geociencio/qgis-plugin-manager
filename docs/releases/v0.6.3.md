# Release v0.6.3 - Test & Code Quality Fixes

## 🐛 Bug Fixes
- **Structural Validation Tests**: Tests in `test_validation_deep.py` failed because `__init__.py` was created empty (no `classFactory` function), violating the stricter validation rules added in v0.6.2. Fixed by writing the minimal `classFactory` stub.
- **Linting `E501`**: Resolved line-too-long violations in `core.py` (`verify_resource_patch`) and `validation.py` (boolean field warning string).
- **Blank-Line Whitespace `W293`**: Removed trailing whitespace on blank lines inside `patch_resource_file` in `core.py`.

## ✨ Additions
- **CI Verification Step**: `uv run qgis-manage --version` step added to the GitHub Actions matrix to verify the CLI installs and runs correctly on Ubuntu, macOS and Windows.
- **Maintenance Documentation**: Added `docs/maintenance/infrastructure_upgrade_20260218.md` summarising the validation-hardening work from this session.

## 🛠️ Upgrade Guide
No breaking changes.
```bash
pip install --upgrade qgis-manage
```
