# Release v0.6.2 - Infrastructure Hardening

## 🚀 Overview
Focus on hardening the infrastructure, improving validation strictness for official QGIS repository compliance, and enhancing the developer experience with better error reporting and resource handling.

## ✨ Highlights
- **Strict Validation**: New checks in `validate` command for metadata compliance (categories, boolean fields, tags).
- **Resource Patching**: Improved `compile` command to automatically patch compiled resource files (`.py`) to fix relative imports and `PyQt5` compatibility issues.
- **Robustness**: Fixed `ConfigParser` case-sensitivity issues and improved `bump` command synchronization.

## 📦 Changes

### Added
- **Advanced Hooks System**: New `hooks` command to manage native Python hooks.
- **Automated Versioning**: New `bump` command supporting Semantic Versioning.
- **Professional Metadata**: Expanded project classifiers and status badges.
- **Tool-only Project Support**: Support for projects without `metadata.txt`.

### Improved
- **Modular CLI Architecture**: Refactored command system for better extensibility.
- **Hierarchical Documentation**: Reorganized docs into specialized subdirectories.
- **RCC Modernization**: Improved dynamic tool detection and import patching.
- **Structural Validation Deepening**: Enhanced strict compliance checks.

### Fixed
- **Resilient Metadata Parsing**: Fixed `ConfigParser` lowercasing keys.
- **Robust Persistence**: Improved `bump` synchronization.
- **Type Safety**: Resolved Mypy type safety errors.
- **Import Resolution**: Fixed relative import issues.
- **Ignore Logic**: Fixed recursive exclusion bugs.

## 🛠️ Upgrade Guide
No breaking changes. Update using:
```bash
pip install --upgrade qgis-manage
```
